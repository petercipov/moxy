-- Up
CREATE TABLE Def (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  def TEXT NOT NULL,
  type TEXT NOT NULL,
  created TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE Mock (
  mockId TEXT PRIMARY KEY ON CONFLICT REPLACE,
  defId INTEGER NOT NULL,
  groupId TEXT NOT NULL,
  FOREIGN KEY (defId) REFERENCES Def (id) 
);

-- Down
DROP TABLE Def;
DROP TABLE Mock;